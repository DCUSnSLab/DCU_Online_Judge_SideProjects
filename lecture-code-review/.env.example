# DB DSN for the DCU Online Judge PostgreSQL.
# Default dev container in devstack/docker-compose.dev.yml uses these creds.
LCR_PG_DSN=postgresql://onlinejudge:onlinejudge@127.0.0.1:5432/onlinejudge

# Optional: override default output root directory (default: ./out)
# LCR_OUT_DIR=./out
